<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.finance.mapper.LoanMapper">

    <select id="getTotalLoanAmountByDate" resultType="java.math.BigDecimal">
        SELECT COALESCE(SUM(AMOUNT), 0)
        FROM LOAN_APPLICATION
        WHERE STATUS_CODE = 'ACTIVE'
        <if test="targetDate != null and targetDate != ''">
            AND DATE(APPLY_DATE) = #{targetDate}
        </if>
    </select>

    <select id="getMockCreditScore" resultType="java.lang.Integer">
        SELECT COALESCE(SCORE, 0)
        FROM MOCK_CB_DATA
        WHERE RESIDENT_NUMBER = #{residentNumber}
    </select>

</mapper>