<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.finance.mapper.LoanMemoMapper">

    <select id="selectMemosByApplicationId" resultType="com.finance.domain.LoanMemo">
        SELECT memo_id as memoId, application_id as applicationId, writer, content, created_at as createdAt
        FROM LOAN_MEMO
        WHERE application_id = #{applicationId}
        ORDER BY created_at DESC
    </select>

    <insert id="insertMemo" parameterType="com.finance.domain.LoanMemo">
        INSERT INTO LOAN_MEMO (application_id, writer, content)
        VALUES (#{applicationId}, #{writer}, #{content})
    </insert>

    <delete id="deleteMemo">
        DELETE FROM LOAN_MEMO WHERE memo_id = #{memoId}
    </delete>

</mapper>