# 기존 org.h2.Driver 등에서 아래로 변경
db.driver=net.sf.log4jdbc.sql.jdbcapi.DriverSpy
# 기존 jdbc:h2:... 등에서 중간에 log4jdbc 추가
db.url=jdbc:log4jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1
db.username=sa
db.password=
hikari.maxPoolSize=10

# Redis Configuration
redis.host=localhost
redis.port=6379